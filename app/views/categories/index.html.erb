<h1>Categories</h1>
<%= link_to 'Create New Category', new_category_path %><br />
<ul>
  <% @categories.each do |category| %>
    <li><%= category.name %> - <%= link_to "Edit", edit_category_path(category) %></li>
  <% end %>
</ul>
